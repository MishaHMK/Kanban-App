package com.kanban.project.errors;

public enum ExceptionMessage {
    EXPIRED_OR_INVALID_TOKEN,
    INVALID_CREDENTIALS,
    USER_NOT_FOUND,
    USER_ALREADY_EXISTS,
    FORBIDDEN,
    SERVICE_UNAVAILABLE,
    VALIDATION_FAILED
}